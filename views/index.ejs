<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body class="px-4 py-2">
		<h1 class="text-2xl italic mb-4"><%= title %></h1>
		<p>Welcome, <%= currentUser.username %></p>
		<p>The rest is coming soon!</p>
		<a href="/logout" class="text-blue-500 underline">Log out</a>
		<% if (currentUser.member) { %>
		<div class="mt-4 flex gap-4">
			<a
				href="/messages/message-board"
				class="rounded-md bg-indigo-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
				>Message Board</a
			>
			<a
				href="/messages/new-message"
				class="rounded-md bg-indigo-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
				>New Message</a
			>
		</div>
		<% } %>
	</body>
</html>
